service pingservice
{
    disable = no
    socket_type = stream
    protocol    = tcp
    wait        = no
    user        = ping
    bind        = 0.0.0.0
    server      = /home/ping/ping.py
    type        = UNLISTED
    port        = 31337
}
